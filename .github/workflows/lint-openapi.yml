name: Lint OpenAPI Specifications

on:
  push:
  pull_request:

jobs:
  spectral:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: stoplightio/spectral-action@latest
        with:
          file_glob: docs/api-specs/*.yaml
